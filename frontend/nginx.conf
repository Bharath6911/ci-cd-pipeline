worker_processes 1;
events { worker_connections 1024; }
http {
  server {
    listen 80;
    root /usr/share/nginx/html;
    location /submit {
      proxy_pass http://20.193.152.161:8080/submit;
    }
  }
}
